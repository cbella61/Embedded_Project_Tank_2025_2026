# Fusion Design Inspector

Script di sola lettura per Autodesk Fusion. Esporta in `cad/reference` un file
JSON con i corpi presenti nel documento attivo e le loro bounding box in mm.

Serve a recuperare le quote indicative dei vecchi componenti cloud prima di
ricostruire i nuovi componenti parametrici e modificabili.
